{% include 'user/structure/header.phtml' %}
{% include 'user/structure/menu.phtml' %}
<div id='content'>
	<p class="detail_adm">{% trans "your" %} {% trans "prof" %}</p>
    <script type="text/javascript">
//        metodo interessante, ma da vedere perchè non funziona
//        $('.confirmation').on('click', function() {
//            return confirm('Are you sure to delete your account???');
//        });
    </script>
    <table class="tablelist">
        <tr>
            <th>{% trans "name" %}</th>
            <th>{% trans "sur" %}</th>
            {% if cus.cod_fis != '' %}
            <th>{% trans "codf" %}</th>
            {%endif%}
            {% if cus.piva != '' %}
            <th>{% trans "piva" %}</th>
            {%endif%}
            <th>{% trans "adr" %}</th>
            <th>{% trans "mail" %}</th>
            <th>{% trans "tel" %}</th>
            <th>{% trans "cel" %}</th>
            <th>{% trans "act" %}</th>
            <th>{% trans "op" %}</th>
            <th>{% trans "type" %}</th>
        </tr>
        <tr>
            <td>{{ cus.name }}</td>
            <td>{{ cus.surname }}</td>
            {% if cus.cod_fis != '' %}
            <td>{{ cus.cod_fis }}</td>
            {%endif%}
            {% if cus.piva != '' %}
            <td>{{ cus.piva }}</td>
            {%endif%}
            <td>{{ cus.address }}</td>
            <td>{{ cus.email }}</td>
            <td>{{ cus.telephone }}</td>
            <td>{{ cus.cellphone }}</td>
            <td>{{ cus.active }}</td>
            <td>{{ cus.operator }}</td>
            <td>{{ cus.type }}</td>
            <td class="delete"><a href='delete.php?id={{ cus.id }}' onclick="return confirm('{% trans "del.profile" %}')"><img src="../../../images/icons/delete/delete-256.png" alt='{% trans "del" %}'></a></td>
            <!-- commentato perchè non vuole modifica utenti, ma noi lo teniamo -->
            <!-- <td class="edit"><a href='prepareUpdate.php?id={{ cus.id }}'><img src="../../../images/icons/edit/edit-256.png" alt='{% trans "edit" %}'></a></td> -->
        </tr>
    </table>
            <p class="detail_add">{% trans "adrs" %}</p>
        <div class="add_add">
		  <a href='prepareInsert_address.php?cus_id={{ cus.id }}'><img alt='{% trans "ins" %} {% trans "new" %} {% trans "adr" %}' src="../../../images/icons/Adds/add_list/add_list-256.png"></a>
	   </div>
        <table class="tablelist">
        <tr>
            <th>{% trans "street" %}</th>
            <th>{% trans "city" %}</th>
            <th>{% trans "prov" %}</th>
            <th>{% trans "zip" %}</th>
            <th>{% trans "country" %}</th>
        </tr>
        <tr>
        {% for addr in addrs %}
            <td>{{ addr.street }}</td>
            <td>{{ addr.city }}</td>
            <td>{{ addr.province }}</td>
            <td>{{ addr.zip }}</td>
            <td>{{ addr.country }}</td>
            <td class="delete"><a href='delete_address.php?cus_id={{ cus.id }}&id={{ addr.id }}'><img src="../../../images/icons/delete/delete-256.png" alt='{% trans "del" %}'></a></td>
        </tr>
        {% endfor %}
    </table>
</div>
{% include 'user/structure/footer.phtml' %}
